# Data Cleanup (Deferred This Week)

Priority 6 is intentionally deferred this week.

## Why deferred
- This week focuses on KD baseline lock, ablation, prewarm, logits recheck, and cross-language analysis.
- Real recording cleanup will be resumed in a later week.

## Reserved Interface (for future execution)
Expected outputs when re-enabled:
- `data_cleanup/cleaned_dataset/`
- `data_cleanup/metadata.csv`
- `data_cleanup/dropped_samples.csv`
- `data_cleanup/cleanup_report.md`
