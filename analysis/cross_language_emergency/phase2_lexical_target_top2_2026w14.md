# Phase2 Lexical Target Top2 2026W14

## Ranking Rule Applied
- Priority score order: lexical_coverage_level > semantic_match_level > sample size > license risk.
- Current constraint: all multilingual candidates are `hold` because transcript/text is unavailable.

## Recommended Top-2 Targets (lexical-first enablement)
1. Italian (EMOZIONALMENTE): strict semantic mapping, best estimated sample scale, low license risk (CC BY 4.0).
2. German (EMO-DB): strict semantic mapping, cleaner license profile (CC0), lower sample size but above estimated minimum gate.

Both are `hold` right now until transcript/text assets are staged; this is a lexical availability blocker, not a mapping blocker.

## Fallback
- French (CaFE): strict mapping and estimated sample gate pass, but non-commercial license risk and transcript/text still unavailable in current assets.
